#pragma once

#include <stdio.h>

void debug(char* msg); // must be printed with white color
void error(char* err_msg); // must be printed with red color
void warning(char* warning_msg); // must be printed with yellow color
